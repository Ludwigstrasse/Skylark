# Skylark Engine Ability Lab

A second test project focused on Skylark engine-side capabilities rather than the original NVIDIA sample parity path.

What it demonstrates:
- GeometryRegistry driven rendering
- Material mode / object-id mode / section tint mode
- Transformed bounds overlay for all draw items
- Camera orbit / zoom
- Fast solid-only default path for interactive inspection
